<h1>Cats!</h1>

<h2>List of Burgers</h2>

<ul>
  {{#each burger}}
    {{#unless devoured}}
      {{> burguer/burguer-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">burger Name:</label>
    <input type="text" id="bu" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>
